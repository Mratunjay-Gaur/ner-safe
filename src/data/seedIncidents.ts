import { IncidentReportItem } from '../types/incident';

// All incidents must be loaded directly from backend API connected to MongoDB.
// Hardcoded client-side seed incidents have been completely eliminated.
export const INITIAL_SEED_INCIDENTS: IncidentReportItem[] = [];

